let config: {
  baseURL: string;
};

if (__DEV__) {
  config = {
    baseURL: "https://nodejs-project-391016.df.r.appspot.com/api/",
  };
} else {
  config = {
    baseURL: "https://nodejs-project-391016.df.r.appspot.com/api/",
  };
}

export default config;
